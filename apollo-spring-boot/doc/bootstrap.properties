
# 注意，经过验证，bootstrap.properties文件并不起作用, application.yml或application.properties配置才起作用

apollo.bootstrap.enabled=true

# 指定 命名空间，默认是 application.properties
apollo.bootstrap.namespaces=ymlValue.yml